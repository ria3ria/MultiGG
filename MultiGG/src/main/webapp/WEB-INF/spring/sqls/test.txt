CREATE TABLE LOLBOARD(
	BOARDKATEGORIE VARCHAR(100) NOT NULL,
	BOARDNO INT AUTO_INCREMENT PRIMARY KEY,
	BOARDNAME VARCHAR(1000) NOT NULL,
	BOARDTITLE VARCHAR(2000) NOT NULL,
	BOARDCONTENT VARCHAR(2000) NOT NULL,
	BOARDDATE DATE NOT NULL,
	BOARDVIEW INT NOT NULL,
	BOARDLIKE INT NOT NULL,
	MEMBERNO INT NOT NULL,
	BOARDMDFDATE DATE NOT NULL
);


DROP TABLE MEMBER;

CREATE TABLE MEMBER(
    MEMBERNO INT AUTO_INCREMENT PRIMARY KEY,
    MEMBEREMAIL VARCHAR(100) NOT NULL UNIQUE,
    MEMBERNICKNAME VARCHAR(100) NOT NULL UNIQUE,
    MEMBERPW VARCHAR(100) NOT NULL,
    MEMBERNAME VARCHAR(100) NOT NULL,
    MEMBERLEVEL VARCHAR(10) NOT NULL,
    MEMBERDATE DATE NOT NULL
);

SELECT * FROM LOLBOARD;
SELECT * FROM MEMBER;

DROPE TABLE COMMENT;

CREATE TABLE COMMENT(
        COMMENTNO INT AUTO_INCREMENT PRIMARY KEY,
	MEMBERNO INT,
        BOARDNO INT,
        COMMENTWRITER VARCHAR(1000) NOT NULL,
        COMMENTTITLE VARCHAR(10000) NOT NULL,
        COMMENTGOOD INT NOT NULL,
        COMMENTBAD INT NOT NULL,
        COMMENTDATE DATE NOT NULL
    );
    
SELECT * FROM COMMENT;


CREATE TABLE COMMENTRECOMMEND(
		MEMBERNO INT PRIMARY KEY,
		COMMENTNO INT NOT NULL,
		RECOMMEND INT NOT NULL
		
	);
